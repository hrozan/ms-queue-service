Server side
1. Get and handle the coming message
2. Add the message into the queue
3. Pika an available Client Worker and assign to the message


Client side
1. List the stored messages


General
*. Create the model to the message (with uuid)
*. Create data structure to model and define the Workers
*. Define the criteria to pick the best Client Worker (e.g. The most beautiful)
